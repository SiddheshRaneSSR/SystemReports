1.Node index.js
2.Grafana - docker run -d --name=grafana-container -p 3000:8000 grafana/grafana-oss    
    //grafana running on port 8000 not default 3000
3.prometheus - docker compose up
    //running prometheus from docker compose taking prometheus.yml file